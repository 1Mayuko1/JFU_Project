import React from 'react';
import styles from "../style";

const NotFound = () => {
    return (
        <div>
            <h1 className={`${styles.paragraph}`}>Hello</h1>
        </div>
    );
};

export default NotFound;
